#include "chart.h"
// init months

void monthsGen(char *arr[12])
{
    for (int i = 0 ; i < 12 ; ++i)
    {
        arr[i] = malloc(4);
    }
    arr[0]
}

void ganttChart()
{
    char *months[12];
    monthsGen(months);
}
